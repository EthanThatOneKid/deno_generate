{
  "tasks": {
    // TODO: Skip cov/ directory.
    "flint": "deno fmt && deno lint",
    "test": "deno test -Ar",
    "bench": "deno bench -Ar",
    "cov": "deno test -Ar --coverage=\"cov\" && deno coverage cov/",
    "e2e": "deno run -Ar main.ts"
  }
}
